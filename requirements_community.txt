# Community Module — Fine di Mondo APS
# Installa con: pip install -r requirements_community.txt

# Web framework (già in requirements_jackass.txt)
fastapi>=0.135.0
uvicorn[standard]>=0.42.0
pydantic[email]>=2.12.0
python-multipart>=0.0.6

# Database (già in requirements_jackass.txt)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=0.21.0

# Auth JWT
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0

# QR Code per check-in eventi
qrcode[pil]>=7.4.2
Pillow>=10.0.0

# GCP & AI (già in requirements_jackass.txt)
google-generativeai>=0.5.0
google-auth>=2.25.0

# WebSocket (già in uvicorn[standard])
websockets>=12.0

# Firebase Admin SDK (verifica Google idToken)
firebase-admin>=6.0.0
